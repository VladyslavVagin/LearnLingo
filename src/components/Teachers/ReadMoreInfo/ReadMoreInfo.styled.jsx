import styled from "styled-components";

export const AddInfoContainer = styled.div`
    margin-top: 16px;
    cursor: pointer;

    p {
        font-size: 16px;
        font-weight: 400;
        line-height: 1.5;
        color: var(--dark-color);
    }
`